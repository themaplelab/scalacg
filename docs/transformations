// Object vs Class
object A => class A, class A$ + a public and static field MODULE$ in A$ to access its methods
class A => class A

// Val vs Var
val a => a() // getter only
var a => a(), a_eq(bla) // setter and getter

// Traits
trait X => interface X, class X$class